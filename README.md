# WDIO_and_Cucumber_Tests
This repository contains an initial setup of WebdriverIO with the Cucumber BDD framework for automated UI testing. It demonstrates the full configuration and execution flow — from environment setup to running parallel headless tests in multiple browsers.
